version: '2'
services:
    scamp-ede:
        build: .
        ports:
            - "5001:5001"
        volumes:
            - .:/app
        depends_on:
            - redis
    redis:
        image: redis